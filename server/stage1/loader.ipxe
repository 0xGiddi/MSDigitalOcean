#!ipxe

# loader.ipxe
# Loads the winboot extention in order to boot the WinPE wim image
# Might want to add in future multiple arch support

kernel wimboot
initrd stage1/boot/bcd          BCD
initrd stage1/boot/boot.sdi     boot.sdi
initrd -n stage1/boot/boot.wim boot.wim
boot
